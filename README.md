#

## Setup macos

```
brew install r
```

run via

```
Rscript file.R
```

### Install dependencies

fire up `R` and run
```
install.packages("ggplot2")
install.packages("ggmap")
install.packages("RColorBrewer")
```
